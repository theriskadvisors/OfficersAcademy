
<script>
    $(document).ready(function () {
        $("#example").dataTable();

        $('.table').on('click', 'tbody tr', function () {
            var td = $(this).children("td");
            var userName = $(td[0]).children("input").val();
            userName = userName.trim();

            window.location.href = "/AspNetFinancePeriods/Details?id=" + userName;
        });

        DrawTable();
    })


    function DrawTable() {
        $('#example_filter').addClass("form-group");
        $('#example_filter').addClass("has-feedback");
        $('#example_filter input').css({ "border": "1px solid #ddd", "border-radius": "40px", "height": "30px", "margin-top": "-5px", "outline": "none", "padding-left": "10px", "color": "#ddd !important" });
        $('#example_filter').append('<span class="glyphicon glyphicon-search form-control-feedback" style="margin-top: -33px; color: #666;"></span>');
        $('#example_length').append(
            '<div class="form-inline" style="margin-left: 140px !important; margin-top: -25px !important; z-index: -3 !important">'
          +
            '&nbsp;&nbsp;&nbsp;&nbsp;' +
            '<div class="form-group">' +
                 '<div class="d-bar"></div>' +
             '</div>' +
             '&nbsp;&nbsp;&nbsp;&nbsp;' +
                 '<div class="form-group">' +
                     '<div class="edit-section">' +
                         '<a href=""><i class="glyphicon glyphicon-repeat" data-toggle="tooltip" title="Refresh" data-placement="bottom"></i></a>' +
                         '<a  href="/AspNetFinancePeriods/Create" id="edit-view-3" data-toggle="tooltip" title="Create" data-placement="bottom">&nbsp;' +
                             '<i class="glyphicon glyphicon-edit"></i>' +
                         '</a>' +
                         '<span style="color:gray"><i class="fa fa-ban"></i>&nbsp;&nbsp;&nbsp;</span>' +
                         '<span style="color:gray"><i class="glyphicon glyphicon-download-alt"></i></span>&nbsp;&nbsp;&nbsp;' +
                    '</div>' +
                 '</div>' +
             '<div class="form-group">' +
                 '<div class="d-bar"></div>' +
             '</div>' +
         '</div>'
         );

    }
</script>




<label>@Html.ActionLink("Home", "Dashboard", "Account") > <a href="#" class="sidebar-toggle navigate" data-toggle="offcanvas" style="background-color:#F7F7F7 !important">User </a> > <a href="#"><b>Finance Period</b></a> </label>

<h4>Finance Periods</h4>
<label id="View">View Default</label>

<div class="box box-primary">
    <div class="box-body">
        @model IEnumerable<SEA_Application.Models.AspNetFinancePeriod>

        @{
            ViewBag.Title = "Index";
        }

        



        <table class="table" id="example">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Year)
                    </th>
                </tr>
            </thead>
            <tbody>

                @foreach (var item in Model)
            {
                    <tr>
                        <td>
                            <input type="hidden" value="@item.Id" />
                            @Html.DisplayFor(modelItem => item.Year)
                        </td>
                    </tr>
                }
            </tbody>

        </table>

    </div>
</div>

@*@model IEnumerable<SEA_Application.Models.AspNetFinancePeriod>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Year)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Year)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>*@
