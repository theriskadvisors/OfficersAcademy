@model IEnumerable<SEA_Application.Models.AspNetFinanceLedgerBalance>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.OldBalance)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NewBalance)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AspNetFinanceLedger.Code)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.OldBalance)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NewBalance)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AspNetFinanceLedger.Code)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
